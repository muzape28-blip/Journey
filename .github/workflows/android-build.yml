# PENTING: salin isi file ini ke .github/workflows/android-build.yml via web UI.
# v5 — 2026-09-03: + RUNTIME SMOKE HEADLESS: CI menjalankan scene utama
#   headless ~4 detik; log BOOT-x/HUD-ALIVE/SCRIPT ERROR di-commit balik ke
#   docs/ci/last_build_manifest.txt + docs/ci/runtime_smoke.log supaya sandbox
#   (yang tak bisa unduh binary/log CI) bisa membaca nasib runtime via git.
name: Android APK (E-strafe)

on:
  workflow_dispatch:
  push:
    branches: ["arena/01a05959-journey"]
    paths-ignore:
      - "docs/ci/last_build_manifest.txt"
      - "docs/ci/runtime_smoke.log"

permissions:
  contents: write

jobs:
  export-android:
    runs-on: ubuntu-latest
    container:
      image: barichello/godot-ci:latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup (templates + java + fontconfig)
        run: |
          set -e
          apt-get update -qq > /dev/null && apt-get install -y -qq fontconfig > /dev/null || true
          GODOT_VER=$(godot --headless --version | cut -d'.' -f1-3)
          echo "Godot terdeteksi: $GODOT_VER"
          mkdir -p ~/.local/share/godot/export_templates ~/.config/godot ~/.android
          if [ -d "/root/.local/share/godot/export_templates/${GODOT_VER}.stable" ]; then
            cp -r "/root/.local/share/godot/export_templates/${GODOT_VER}.stable" ~/.local/share/godot/export_templates/
          else
            wget -q "https://github.com/godotengine/godot/releases/download/${GODOT_VER}-stable/Godot_v${GODOT_VER}-stable_export_templates.tpz" -O /tmp/tpz
            mkdir -p ~/.local/share/godot/export_templates/${GODOT_VER}.stable
            unzip -j -q /tmp/tpz "templates/android_debug.apk" "templates/android_release.apk" -d ~/.local/share/godot/export_templates/${GODOT_VER}.stable/
          fi
          ls ~/.local/share/godot/export_templates/${GODOT_VER}.stable | head -5
          if [ ! -f ~/.android/debug.keystore ]; then
            keytool -genkeypair -keystore ~/.android/debug.keystore -storepass android \
              -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 \
              -validity 10000 -dname "CN=Android Debug,O=Android,C=US"
          fi
          ES=~/.config/godot/editor_settings-4.tres
          if [ -f /root/.config/godot/editor_settings-4.tres ]; then
            cp /root/.config/godot/editor_settings-4.tres $ES
          fi
          if [ ! -f $ES ]; then
            printf '[godot_resource type="EditorSettings" format=3]\n\n[resource]\n' > $ES
          fi
          JAVA_DIR=$(dirname "$(dirname "$(readlink -f "$(command -v java)")")")
          {
            echo "export/android/java_sdk_path = \"$JAVA_DIR\""
            echo "export/android/debug_keystore = \"$HOME/.android/debug.keystore\""
            echo "export/android/debug_keystore_user = \"androiddebugkey\""
            echo "export/android/debug_keystore_pass = \"android\""
          } >> $ES
          cp $ES ~/.config/godot/editor_settings-${GODOT_VER%.*}.tres
          echo "JAVA_HOME=$JAVA_DIR" >> "$GITHUB_ENV"

      - name: Import aset (headless)
        run: |
          mkdir -p build
          godot --headless --path . --import || true

      - name: Runtime smoke headless (log masuk repo)
        run: |
          timeout 30 godot --headless --path . res://scenes/main.tscn --quit-after 240 > docs/ci/runtime_smoke.log 2>&1 || true
          echo "=== SMOKE LOG ==="
          cat docs/ci/runtime_smoke.log

      - name: Export APK (debug, universal)
        run: |
          godot --headless --path . --export-debug "Android" build/Estrafe.apk
          ls -la build/

      - name: Otopsi APK + smoke → commit balik
        run: |
          {
            echo "commit: $(git rev-parse --short HEAD)"
            echo "ukuran: $(stat -c%s build/Estrafe.apk) byte"
            echo "--- lib/ ---"
            unzip -l build/Estrafe.apk | grep -E "lib/.*\.so" || echo "(TIDAK ADA lib/!)"
            echo "--- sha256 ---"
            sha256sum build/Estrafe.apk
          } > docs/ci/last_build_manifest.txt
          cat docs/ci/last_build_manifest.txt
          git config user.name "estrafe-ci-bot"
          git config user.email "ci-bot@users.noreply.github.com"
          git add docs/ci/last_build_manifest.txt docs/ci/runtime_smoke.log
          git commit -q -m "ci: manifest+smoke build $(git rev-parse --short HEAD) [skip ci]" || echo "tak ada perubahan"
          git push origin HEAD:arena/01a05959-journey || echo "push manifest gagal (non-fatal)"

      - name: Upload artifact (zip, cadangan)
        uses: actions/upload-artifact@v4
        with:
          name: e-strafe-apk
          path: build/*.apk

      - name: Rilis .apk mentah (tautan unduh langsung)
        uses: softprops/action-gh-release@v2
        with:
          tag_name: e-strafe-latest
          name: "E-strafe APK (auto)"
          prerelease: true
          files: build/Estrafe.apk
          body: |
            Build otomatis dari push terakhir.
            Unduh file **Estrafe.apk** di bawah (jangan yang artifact-zip).
